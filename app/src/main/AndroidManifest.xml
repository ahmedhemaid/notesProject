<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ChooseCategoryStyleActivity"></activity>
        <activity android:name=".Activities.AddNewNoteActivity" />
        <activity
            android:name=".Activities.NoteBooksShowAll"
            android:launchMode="singleTask" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".Activities.LoginActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".Activities.ForgetPassActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".Activities.SplashActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".Activities.NotesShowAll"
            android:launchMode="singleTask" />
        <activity android:name=".Activities.EditNoteActivity" />
        <activity android:name=".Activities.HomeActivity">
                 </activity>
        <activity android:name=".Activities.NoteTutorial2" />
        <activity android:name=".Activities.AddNewCategoryActivity" />
        <activity android:name=".Activities.GoToEmail"/>
        <activity
            android:name=".Activities.NoteTutorial1"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>