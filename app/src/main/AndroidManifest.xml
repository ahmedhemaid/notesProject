<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.NoteBooksShowAll" android:launchMode="singleTask"></activity>
        <activity android:name=".Activities.RegisterActivity"  android:launchMode="singleTask"/>
        <activity android:name=".Activities.LoginActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.ForgetPassActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.SplashActivity" android:launchMode="singleTask"/>
        <activity android:name=".Activities.NotesShowAll" android:launchMode="singleTask">
        </activity>
        <activity android:name=".Activities.HomeActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.NoteTutorial2"></activity>
        <activity
            android:name=".Activities.NoteTutorial1" android:launchMode="singleTask"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>